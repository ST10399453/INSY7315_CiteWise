@using CiteWise_Web.Models.Account
@model ConsultantOnboardingModel

@{
    ViewData["HideLayoutElements"] = true;
}

<div class="register-page">
    <div class="register-container">
        <div class="logo-header">
            <img src="~/img/CiteWise_Logo.jpg" alt="CiteWise Logo" class="logo" />
            <h2 class="app-name">iteWise</h2>
        </div>

        <h2 class="heading">Consultant Onboarding</h2>

        <div class="register">
            <div class="forms-stage show-register">
                <div class="form-panel register-panel">                   
                    <form asp-action="Consultant" method="post" class="register-form">
                        @Html.AntiForgeryToken()

                        <input type="hidden" asp-for="Uid" />
                        <input type="hidden" asp-for="IdToken" />
                        <div class="form-group">
                            <label asp-for="Language"></label>
                            <input asp-for="Language" class="form-input" />
                            <span asp-validation-for="Language" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Specialisation"></label>
                            <input asp-for="Specialisation" class="form-input" />
                            <span asp-validation-for="Specialisation" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn-primary">Save</button>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
